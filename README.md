# Visualization of a planary graph

Simple program that calculates coordinates of vertices in a weighted graph. It uses two different algorithms, Tutte's embedding and Fruchtermana-Reingold's algorithm.

## Basic C visualizer usage:
./visual -i input-file -s scale -w window-width -h window-height
